# Semua variabel di sini WAJIB diawali VITE_ dan di-bake ke bundle statis saat build
# (bukan dibaca saat runtime) -- ini konsekuensi dari Vite/React sebagai static site.

# Base URL backend. KOSONGKAN -- nginx di container frontend (lihat nginx.conf)
# reverse-proxy /api/status-pages/:slug ke backend lewat docker network internal,
# jadi browser cukup fetch relatif ke domain frontend sendiri. Backend tidak pernah
# expose domain publik-nya sendiri.
VITE_API_BASE_URL=

# Judul halaman utama "/" (gabungan semua status page yang di-toggle "tampil di
# halaman utama" lewat /admin). Status page individual tetap punya URL sendiri
# di /<slug> apa pun isi variabel ini. Juga dipakai buat <title>/og:title.
VITE_HOME_TITLE=Status Layanan

# Meta description / og:description -- muncul di hasil pencarian & preview link
# (WhatsApp/Telegram/dll) waktu link status page ini di-share.
VITE_HOME_DESCRIPTION=Status realtime layanan kami

# Interval polling ke backend, dalam milidetik
VITE_POLL_INTERVAL_MS=20000

# Key periode uptime yang ditampilkan sebagai badge persentase di tiap baris monitor.
# Cek response GET /api/status-pages/:slug -> monitors[].live.uptime buat lihat
# key apa saja yang tersedia dari Kuma (biasanya "24" = 24 jam terakhir).
VITE_UPTIME_PERIOD_KEY=24

# Key + label periode buat tile "Uptime" di kartu ringkasan (rata-rata semua monitor).
# "720" = 30 hari (720 jam), sesuai konvensi periode yang dikirim Kuma.
VITE_HEADLINE_PERIOD_KEY=720
VITE_HEADLINE_PERIOD_LABEL=30 hari
